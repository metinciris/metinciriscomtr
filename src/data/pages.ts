/**
 * validPages artık PAGE_REGISTRY'den türetiliyor.
 * Bu dosya geriye dönük uyumluluk için korunuyor.
 * Doğrudan import için: import { validPages } from '@/core/data/registry';
 */
export { validPages } from '@/core/data/registry';
